# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
tfplan
tfplan*
*.tfplan
crash.log

# Lambda build artifact (regenerated by terraform)
**/lambda/*.zip

# Scratch plan copies
/evidence_plan.json
/red_plan.json
